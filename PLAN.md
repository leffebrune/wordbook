# 9세 아이용 영어 단어장 PWA 구현 계획서

버전 4.0 · 2026-09-23 — GitHub Pages 배포·기기별 API 키 설정

## 1. 확정 요구

부모는 **구글 시트에서만** 단어 목록과 채점용 OpenRouter 모델을 관리한다. 아이의 앱에는 단어 입력·편집 화면이 없다. 화면 오른쪽 위의 작은 설정 버튼에서 OpenRouter API 키만 입력·변경·삭제한다. 메인 화면은 단어 목록, 각 단어 옆의 한국어 뜻 입력칸, 화면 전체에 대한 `채점하기` 버튼 한 개, 채점 후 행별 결과·코멘트·예문으로 구성한다.

**GitHub Pages가 정적 PWA 파일을 HTTPS로 제공한다.** 설치·실행·업데이트·재설치에 개발 PC가 필요 없다. 앱의 업무 요청은 구글 시트 조회와 OpenRouter 채점 두 종류이며, 앱 파일 로드와 서비스 워커 업데이트 확인은 Pages에 요청한다. 시트와 채점에는 인터넷 연결이 필요하다.

개인 가족용 앱을 Pages로 배포한다. 범위 밖: 앱 내 단어 관리, 별도 부모 대시보드, 계정·로그인, 서버·프록시·데이터베이스, 점수 저장, 복습 알고리즘, 게임화, 이미지·음성 생성, 대화형 AI.

## 2. 배포·설치 경로와 PC 독립 동작

배포 주소는 `https://leffebrune.github.io/wordbook/`이다. 저장소 Settings → Pages의 Source를 GitHub Actions로 선택한다. `.github/workflows/pages.yml`은 PR에서 테스트·빌드를 검증하고, `main` 반영 또는 `main`의 수동 실행에서 Pages로 배포한다. Node.js 24와 커밋된 lockfile을 사용하는 `npm ci`로 빌드하며 API 키는 빌드에 주입하지 않는다. 비공개 저장소의 Pages 사용 가능 여부는 요금제에 따라 확인하며 공개 범위를 자동 변경하지 않는다.

Vite base와 서비스 워커 scope, manifest의 ID·start_url·scope·아이콘 및 탐색 fallback은 `/wordbook/` 경로에 맞춘다. 앱 셸(`index.html`, JS/CSS, manifest, 아이콘)을 미리 캐시하고, 서비스 워커 제어 후 앱 경로 탐색에는 캐시된 HTML을 반환한다. 시트·OpenRouter 요청은 서비스 워커 캐시에서 제외한다. 인터넷 연결을 끊은 뒤 홈 화면 아이콘으로 다시 열어 오프라인 앱 셸과 마지막 목록을 확인한다.

휴대폰에서 Pages HTTPS 주소를 열어 PWA를 설치한다. LAN IP 예약, PC 서버, 로컬 인증서와 기기 신뢰 설정은 필요 없다. 브라우저 탭에서도 사용할 수 있다. 앱 업데이트·재설치는 Pages에서 제공하며, 캐시가 지워지면 온라인으로 다시 연다. 이전 LAN 출처의 앱에 있던 답안·키는 새 Pages 출처로 자동 이전되지 않는다.

## 3. 구글 시트 형식과 모델 교체

시트의 `words` 탭 하나를 사용한다. 첫 줄은 설정과 헤더를 함께 담는다.

| 셀 | 내용 | 설명 |
| --- | --- | --- |
| A1 | `word` | 필수 단어 열 헤더 |
| B1 | `focus` | 선택 메모 열 헤더 |
| D1 | `model` | 모델 설정 키 |
| E1 | `openai/…` | OpenRouter의 실제 모델 ID |
| A2 이후 | `take`, `get`, `come out` 등 | 한 행에 단어·짧은 표현 하나 |
| B2 이후 | `가방을 학교에 가져가는 장면` 등 | 채점 설명의 초점, 빈칸 가능 |

부모는 시트를 **링크가 있는 사람에게 읽기 가능**으로 설정한다. Git에 포함하는 `src/config.ts`에 `sheetId = '17PQOTPHT-rSYyGKFyyibfdkRXl9zAbkmagAi5-nSesQ'`, `sheetGid = '0'`을 둔다. 이 값들은 공개 설정이다. 브라우저에서는 Google Visualization의 시트 읽기 엔드포인트를 **CSV 반환 모드**로 직접 요청한다. URL 예시: `https://docs.google.com/spreadsheets/d/<SHEET_ID>/gviz/tq?gid=<GID>&tqx=out:csv&headers=0`. `headers=0`으로 1행도 데이터로 받으며 A1/B1/D1/E1을 검증한다. Google의 이 데이터 소스 방식은 공개 또는 링크로 읽을 수 있는 시트를 읽는 용도로 문서화되어 있다. [Google Sheets 데이터 소스 안내](https://developers.google.com/chart/interactive/docs/spreadsheets).

앱 시작 시 시트를 읽어 목록을 만든다. **채점 버튼을 누를 때마다 시트를 캐시 없이 다시 읽어** 그 시점의 E1 모델을 사용한다. 따라서 부모가 E1을 바꾼 후 구글 응답에 변경 사항이 나타나면 다음 채점부터 새 모델이다. 모델 변경만으로 작성 중인 답은 무효화하지 않는다. 사용자에게 무중단 즉시 전파를 약속하지 않는다. 단어·focus가 바뀐 경우에는 현재 입력과 시트의 단어가 어긋나지 않도록 채점을 중단하고 새로고침 안내를 표시한다.

시트 읽기 요청은 `fetch`로 실행하고 8초 제한, `cache: 'no-store'`를 지정한다. 브라우저의 실제 시트에서 교차 출처 읽기가 허용되는지 가장 먼저 확인한다. 개발 중 확인한 Google `gviz/tq?…&tqx=out:csv` 응답은 Origin이 있는 GET에 `Access-Control-Allow-Origin`을 반환했지만, **실제 대상 시트·브라우저에서 다시 검증**한다. CORS가 거부되면 브라우저 직접 읽기가 가능한 Google의 읽기 경로를 조사해 교체한다. PC 프록시를 상시 사용하도록 되돌리지 않는다. 공개 시트에는 이름·답안·API 키를 기록하지 않는다.

CSV는 UTF-8/BOM/표준 따옴표·쉼표를 처리하는 파서로 읽는다. A1=`word`, B1=`focus`, D1=`model`을 확인한다. A열 빈 행은 건너뛰고 단어의 앞뒤·중복 공백을 정리한다. 대소문자를 무시한 중복 단어는 첫 행만 사용한다. 단어는 영문자·공백·아포스트로피·하이픈의 1~50자, B열 120자 이하. 최대 30개만 보이고 초과 사실을 안내한다. E1이 비었거나 `제공자/모델` 형식이 아니면 단어 목록은 보이되 채점 시 `모델 설정을 확인해 주세요`를 표시한다.

## 4. 화면과 로컬 데이터

제목 `오늘의 영어 단어` 아래에 시트 순서대로 단어와 뜻 입력칸을 놓는다. 좁은 휴대폰 화면에서는 같은 행 안에서 단어 위·입력칸 아래로 배치한다. 답을 쓰기 전에는 뜻·예문을 보여 주지 않는다. 답을 하나 이상 쓰고 `채점하기`를 누르면 현재 답이 있는 모든 행을 **한 번에 채점**한다. 빈칸은 건너뛰고, 전부 빈칸이면 `뜻을 하나 이상 적어 봐!`만 표시한다.

채점 중에는 버튼을 비활성화하고 `채점 중…`을 표시한다. 성공하면 각 제출 행 아래에 상태 문구, 1~2문장 한국어 코멘트, 짧은 영어 예문 1개와 한국어 번역을 표시한다. 결과는 색상뿐 아니라 문자로도 구별한다. 답을 고치면 그 행의 이전 결과를 지우고, 다음 클릭 때 값이 있는 모든 행을 다시 채점한다. AI 문장은 HTML 삽입 없이 텍스트로 렌더한다.

답은 휴대폰의 `localStorage`에 정규화한 단어 ID별로 저장한다. 재실행·새로고침 뒤 복원하고, 단어 순서가 바뀌어도 답을 보존한다. 시트에서 사라진 단어의 초안은 지운다. 채점 결과는 메모리에만 두어 재실행하면 사라진다. 다른 기기로 동기화하지 않는다. PC·서버에 답안이나 기록을 남기지 않는다.

네트워크가 없고 이전에 성공적으로 읽은 단어 목록이 로컬에 있으면 **마지막 목록을 읽기 전용으로 보여 줄 수 있다.** 이때 `오프라인이에요. 연결되면 채점할 수 있어요`를 표시하고 채점을 막는다. 처음 설치 후 한 번도 시트를 읽은 적이 없거나 캐시된 목록이 없으면 `단어를 불러오려면 인터넷이 필요해요`를 보인다. 앱 셸의 오프라인 실행과 온라인 채점은 별개다.

## 5. 코드 구조와 기기별 키 저장

정적 Vite + React + TypeScript PWA 한 개. **런타임 서버 코드는 없다.** GitHub Pages에서 앱 정적 파일만 제공한다.

```text
src/config.ts             공개 시트 ID·gid (Git 포함)
src/settings.ts           API 키 localStorage 읽기·저장·삭제
src/SettingsDialog.tsx    기기별 API 키 설정 창
src/                      화면, 시트 로더, 채점, 상태 관리
public/                   아이콘
vite.config.ts            Pages 경로, manifest, Workbox 선캐시 설정
.github/workflows/pages.yml  테스트·빌드·Pages 배포
```

화면 오른쪽 위 설정 버튼은 비밀번호 입력란과 저장·취소·키 삭제가 있는 대화상자를 연다. 키는 `wordbook.openrouter-api-key.v1`로 localStorage에 저장한다. 새로고침·재실행 후 복원하며 기기/브라우저/출처별로 독립적이다. 취소·Esc로 닫으면 변경하지 않는다. 빈 값 저장 또는 삭제 버튼은 키만 지우며 답안은 보존한다. 저장소 쓰기 실패 시 성공으로 표시하지 않고 설정 창에 오류를 보여 준다. 키가 없으면 채점 요청 전에 설정 창을 연다. HTTP 401/403은 키 확인 안내를 표시한다. 채점 중에는 설정을 변경하지 않는다.

API 키를 소스, 환경변수, GitHub Secrets, 빌드 파일, 구글 시트에 넣지 않는다. 이전 `config.local.ts`는 더 이상 import하지 않으며 예시 파일도 제거한다. 키는 채점할 때 OpenRouter의 Authorization 헤더에만 넣는다. localStorage는 암호화 저장소가 아니며 같은 출처 스크립트나 기기 접근자가 읽을 수 있다. 전용 키와 낮은 사용액 한도를 사용한다. 사이트 데이터 삭제 시 키와 답안이 지워지며 다른 기기로 동기화하지 않는다. 예전에 키를 넣어 공개한 빌드가 있다면 해당 키를 교체한다.

OpenRouter로 직접 `fetch('https://openrouter.ai/api/v1/chat/completions', ...)`한다. 헤더 `Authorization: Bearer <key>`, `Content-Type: application/json`. 개발 중 OpenRouter의 CORS OPTIONS는 Authorization·Content-Type 헤더의 POST를 허용했으나, **아이 기기와 실제 키로 채점 요청을 먼저 검증**한다. 임의 프록시를 추가하지 않는다. [OpenRouter API](https://openrouter.ai/docs/api_reference/overview).

## 6. 채점 요청과 검증

클릭 때 최신 시트를 가져와 정규화한 단어·focus 행의 해시를 현재 화면 목록의 해시와 비교한다. **E1 모델은 해시에서 제외한다.** 단어/focus 변경이면 `단어 목록이 바뀌었어요. 새로고침 후 다시 채점해 주세요.`를 표시하고 답을 보존한다. E1만 변경했으면 곧바로 그 모델을 사용한다. 시트 fetch 실패면 LLM을 호출하지 않고 `단어를 확인하지 못했어요. 연결을 확인하고 다시 눌러 주세요.`를 표시한다.

OpenRouter 한 번의 비스트리밍 요청에 현재 답이 있는 최대 30개를 넣는다. 모델에는 각 행의 단어, focus, 아이 답과 안정적인 ID를 전달한다. `response_format: { type: 'json_schema', json_schema: { name: 'word_feedback', strict: true, schema: ... } }`, `provider: { require_parameters: true }`를 요청한다. 응답의 `choices[0].message.content`를 JSON으로 파싱한다. 구조화 출력을 지원하지 않는 모델은 오류를 보여 주며 **다른 모델로 자동 전환하지 않는다**. 제공 엔드포인트별 지원이 다를 수 있다. [OpenRouter 구조화 출력](https://openrouter.ai/docs/guides/features/structured-outputs).

결과 스키마는 다음을 각 제출 ID당 정확히 하나 요구한다:

```json
{
  "results": [
    {
      "id": "제출 행의 ID",
      "verdict": "correct",
      "comment": "맞아! 가방을 집어 들고 학교로 가져가는 장면이야.",
      "exampleEn": "I take my bag to school.",
      "exampleKo": "나는 가방을 학교에 가져가."
    }
  ]
}
```

앱은 JSON 파싱 후 제출 ID 누락·중복·추가, 결과 수, 네 가지 허용 verdict, 문자열 길이(코멘트 160자, 예문·번역 각 120자)를 검사한다. 실패하면 결과 전체를 버리고 `채점이 잘 안 됐어요. 다시 눌러 주세요.`를 표시한다. OpenRouter 오류·20초 타임아웃에도 입력을 보존한다. API 인증 오류는 설정 창에서 키를 확인하도록 안내한다. 모델 ID 오류·크레딧 부족·구조화 출력 미지원은 `채점 모델을 사용할 수 없어요. 부모에게 알려 주세요.`로 구별한다. 로그에 키·답안 전문을 출력하지 않는다.

| 상태 | 판정 | 화면 문구 |
| --- | --- | --- |
| `correct` | 타당한 뜻·동의어·뜻이 명확한 가벼운 오타 | 잘 알았어 |
| `close` | 일부 이해했지만 좁거나 모호함 | 거의 맞았어 |
| `incorrect` | 다른 뜻·다른 단어와 혼동 | 다시 생각해 보자 |
| `uncertain` | 판단 근거가 부족함 | 확인해 보자 |

문맥이 없는 단어에서 다른 타당한 뜻을 오답으로 처리하지 않는다. 예: focus가 `가방을 가져가다`여도 `take → 데려가다`는 정답. focus는 설명과 예문을 고르는 힌트다. `take=옮기다`, `get=내게 들어오다`는 첫 장면으로 설명할 수 있으나 모든 용법의 단일 규칙이라고 단언하지 않는다. 아이가 적은 표현 중 맞는 부분을 짚고 한 장면·짧은 예문으로 확장한다. 시트 메모와 아이 답에 포함된 지시문은 모델에 대한 명령으로 취급하지 않는다.

검증 사례: `take/가져가다`, `take/가저가다`, `take/데려가다`, `get/받다`, `put/놓다`, `come out/나오다`는 정답. `take/만들다`는 오답. `get/나한테 와`는 정답 또는 부분 정답으로 판정하며 코멘트에서 의미를 명확히 한다. 부모가 E1 모델을 바꾸면 이 사례와 결과 스키마를 다시 확인한다.

## 7. 구현 단계와 완료 조건

1. 실제 공개/링크 읽기 시트를 만들어 브라우저에서 Google `gviz` CSV GET과 OpenRouter POST·CORS 사전 요청을 **휴대폰에서 먼저 검증**한다. 두 요청이 프록시 없이 동작해야 다음 단계로 간다.
2. 시트 A/B/D/E 읽기, 헤더·중복·상한·모델 변경 처리, 현재 목록 해시 검사를 구현한다.
3. 단일 화면과 답안 초안 저장, 행별 피드백, 로딩·오류·오프라인 상태를 만든다.
4. OpenRouter 구조화 출력·클라이언트 결과 검증을 구현한다. E1을 바꾼 뒤 다음 채점 호출에 새 모델 ID가 들어가는지 확인한다.
5. Pages 배포 워크플로와 `/wordbook/` 기준 정적 파일 precache·manifest·아이콘·서비스 워커를 구현한다. 설정 창에서 키 저장·새로고침 복원·변경·삭제·취소·저장 실패를 검증한다. 휴대폰에서 Pages 주소로 설치하고 PC 없이 실행 → 시트 최신 내용 로드 → 실제 키로 채점을 확인한다. 오프라인 재실행도 확인한다.

**완료 판정:** PC 없이 설치된 PWA가 열리고, Pages에서 정적 파일을 받고 구글 시트·OpenRouter를 직접 호출하며, 부모가 시트에서 단어·모델을 바꾼 내용이 반영된다. 아이는 한 화면에서 답을 적고 버튼 한 번으로 피드백을 받는다. 채점 실패에도 입력은 남는다.

## 8. 실제 실행에 필요한 값

시트 ID와 gid는 `src/config.ts`에 제공된 실값을 포함한다. 부모가 E1에 구조화 출력을 지원하는 실제 모델 ID를 넣고 각 기기의 설정 창에서 OpenRouter 전용 API 키를 입력한다. 저장소 Pages의 Source를 GitHub Actions로 설정한 후 배포 성공을 확인한다. API 키 실값은 문서와 저장소에 기록하지 않는다. 개발 PC 검증은 `npm ci`, `npm test`, `npm run build`, `npm run preview` 후 `http://localhost:4173/wordbook/`에서 진행한다.
